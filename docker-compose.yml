version: '3.8'

services:
  # Your Node.js Application Only
  app:
    build: .
    container_name: urlshortener-app
    restart: unless-stopped
    environment:
      - PORT=3000
      - MONGODB_URI=mongodb://host.docker.internal:27017/urlshortener
    ports:
      - "3000:3000"